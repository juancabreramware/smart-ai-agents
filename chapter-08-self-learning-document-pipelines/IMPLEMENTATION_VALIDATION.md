# Implementation Validation v0.1.0

This release implements the Chapter 8 Experimental Specification v1.0 architecture and benchmark harness. Mock runs validate routing mechanics only; they are not publication evidence.

Before canonical evidence: generate corpus, run tests, audit mock 150, run real 15-document demo, fix genuine implementation defects with regression tests, pin provider/model/pricing in the manifest, then run canonical real 150 once.
