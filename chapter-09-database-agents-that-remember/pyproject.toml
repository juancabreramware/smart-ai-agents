[project]
name = "chapter-09-database-agents-that-remember"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["openai>=1.68.0"]
[project.optional-dependencies]
dev = ["pytest>=8.0"]
[tool.pytest.ini_options]
pythonpath = ["."]
